-- OpEx (5600–5699) and other-income (4200–4299) CoA codes are created per shop
-- via ChartOfAccountsService / expense seed-defaults (not global INSERT — multi-tenant).
-- This migration documents the reserved ranges and is intentionally a no-op marker.
SELECT 1;
